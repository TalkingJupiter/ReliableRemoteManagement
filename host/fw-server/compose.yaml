services:
  fw-server:
    image: nginx:alpine
    ports:
      - "8080:80"
    volumes:
      - "./nginx.conf:/etc/nginx/conf.d/default.conf:ro"
      - "./bin:/usr/share/nginx/fw:ro"
    restart: unless-stopped